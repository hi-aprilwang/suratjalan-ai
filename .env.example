# Optional: Google Gemini API Key for Live Multimodal VLM
# If left blank, system automatically uses deterministic offline fallback engine
GEMINI_API_KEY=your_gemini_api_key_here

# Backend configuration
MOCK_MODE=false
PORT=8000
